import React from 'react';
import { HeaderContainer, HeaderWrapper, Title } from './styles';

const Header = () => {
  return (
    <HeaderContainer>
      <HeaderWrapper>
        <Title>aha q&a</Title>
      </HeaderWrapper>
    </HeaderContainer>
  );
};

export default Header;
